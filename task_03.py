#!/usr/bin/env python
# -*- coding: utf-8 -*-
"""Third task of week 11"""

import produce

class Apple(produce.Produce):
    """Object storing apple's duration
    
    Arguments:
        Produce(object): which produce is it
    Attributes:
        duration(int): duration of the produce
    """
    
    duration = 5356800